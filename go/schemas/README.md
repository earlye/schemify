These demo schemas _look_ like they're violating the spirit of schemify, because there are
different versions. You can think of this as an exploded set of branches, to easily let folks
see how schemify works, rather than how you would use it in a dev project.

Proper usage would have just a single schema directory and some mechanism to trigger schemify
as part of CD processes.